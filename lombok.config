# Sin esto busca un archivo de configs en cada sub carpeta
config.stopBubbling = true

# Sin esto JaCoCo te pide coverage de getters, setters y constructores
lombok.addLombokGeneratedAnnotation = true
